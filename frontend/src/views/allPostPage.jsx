import axios from 'axios'

export default function AllPost(){
    return (
        <div>
            {/* {data.map(e=>{
                return <p key={e.id}>{e.id}</p>
            })} */}
        </div>
    )
}